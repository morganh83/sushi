[project]
name = "sushi"
version = "0.1.1"
description = "Mobile RFID clone controller for Doppelganger Core + Proxmark3"
requires-python = ">=3.10"
dependencies = [
    "starlette>=0.40.0",
    "uvicorn>=0.32.0",
    "wsproto>=1.0.0",
    "httpx>=0.28.0",
]
