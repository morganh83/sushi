[project]
name = "sushi"
version = "0.1.0"
description = "Mobile RFID clone controller for Doppelganger Core + Proxmark3"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.28.0",
]
